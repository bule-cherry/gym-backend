<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clz.web.member.mapper.MemberMapper">
    
    <update id="addMoney" parameterType="com.clz.web.member.entity.RechargeParam">
        update member set money = money + #{param.money} where member_id = #{param.memberId}
    </update>

    <update id="subMoney" parameterType="com.clz.web.member.entity.RechargeParam">
        update member set money = money - #{param.money} where member_id = #{param.memberId}
    </update>

</mapper>