<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.clz.web.member_recharge.mapper.MemberRechargeMapper">

    <select id="getRechargeList" resultType="com.clz.web.member_recharge.entity.MemberRecharge">
        SELECT mr.*,m.`name`,m.username FROM
            member_recharge mr LEFT JOIN member m ON mr.member_id = m.member_id
    </select>
    <select id="getRechargeByMember" resultType="com.clz.web.member_recharge.entity.MemberRecharge">
        SELECT mr.*,m.`name`,m.username FROM
            member_recharge mr LEFT JOIN member m ON mr.member_id = m.member_id
            WHERE m.member_id = #{memberId}
    </select>
</mapper>